import React from "react"

import "./app-next.scss"

function AppNext() {
  return (<div className="get-in-touch__next">
  <h2 className="get-in-touch__next--title">What's next</h2>
  <p className="get-in-touch__next--text">We’ll contact you within a few hours with our next steps. We normally schedule a call with our engineers to discuss your project in more detail. If you’d like to sign an NDA, please let us know. We’ll prepare it for you.</p>
  <p className="get-in-touch__next--text"> Since we live on two different continents (Australia and Europe) we are available around the clock.</p>
</div>
    )
};

export default AppNext;